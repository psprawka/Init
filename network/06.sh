# 6. Get the complete path of the file that contains the IP address of the DNS server you’re using

# this file contains IP address of my DNS server, could as well use scutils and grep DNS's address
/etc/resolv.conf 
